<!DOCTYPE html>
<html lang="en">
{{> head}}
<body>
    {{> header}}
    <div class="container">
        <h1>Tests for {{tests.[0].chapter_name}}</h1>
        <ul>
            {{#each chapters}}
                <li>
                    <a href="/test/{{this.id_hash}}">{{this.name}}</a>
                </li>
            {{/each}}
        </ul>
    </div>
    {{> footer}}
</body>
</html>
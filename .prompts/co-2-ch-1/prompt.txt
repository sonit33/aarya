You are an expert teacher and course author in AP Computer Science A.

I need your help to generate 5 question from Unit 1 (Primitive Types) from the topics below of the AP Computer Science A syllabus
The question will have difficulty ranging from 1 to 5.
1 is the easiest and 5 is the most difficult question.

Unit 1 topics is as follows:
1.1 Why Programming? Why Java?
1.2 Variables and Data Types
1.3 Expressions and Assignment Statements
1.4 Compound Assignment Operators
1.5 Casting and Ranges of Variables

Respond in a json document (without code block formatting) that conforms to the following schema:
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "que_text": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2048
      },
      "choices": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string"
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "id",
            "text"
          ]
        }
      },
      "answers": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string"
            }
          },
          "required": [
            "id"
          ]
        }
      },
      "ans_explanation": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2048
      },
      "ans_hint": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1024
      },
      "que_difficulty": {
        "type": "integer",
        "minimum": 1,
        "maximum": 5
      },
      "diff_reason": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1024
      }
    },
    "required": [
      "que_text",
      "choices",
      "answers",
      "ans_explanation",
      "ans_hint",
      "que_difficulty",
      "diff_reason"
    ]
  }
}

The question may have 4 to 5 choices.
The question may have 1 or more correct answers from the given choices.
Provide succinct explanation and reason highlighting the key concepts involved.
All the string fields will use a markdown syntax.
Use MathML for mathematical expressions.
Only generate the json response that I can copy without any explanation.
